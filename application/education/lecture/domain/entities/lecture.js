export default class Lecture {
  constructor(title, description, authorId, tasks) {
    this.title = title;
    this.description = description;
    this.authorId = authorId;
    this.tasks = tasks;
  }
}
